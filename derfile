# [https://github.com/ThyW/der/]
$hosts = legionnaire,terminator,pyromancer,thief
$outdir = env`HOME`

# alacritty
[alacritty/alacritty.yml.t]
hostnames = $hosts
final_name = alacritty.yml
apply_path = $outdir:/.config/alacritty/

# .bashrc
[bash/bashrc]
hostnames = $hosts
final_name = .bashrc
apply_path = $outdir

# .tmux.conf
[bash/tmux.conf]
hostnames = $hosts
final_name = .tmux.conf
apply_path = $outdir

# .gitignore
[bash/.gitignore]
hostnames = $hosts
final_name = .gitignore
apply_path = $outdir

[bash/ghci.conf]
hostnames = $hosts
final_name = .ghci
apply_path = $outdir

# starship.toml
[bash/starship.toml]
hostnames = $hosts
final_name = starship.toml
apply_path = $outdir:/.config/

# dunstrc
[dunst/dunstrc]
hostnames = $hosts
final_name = dunstrc
apply_path = $outdir:/.config/dunst/

[i3]
hostnames = $hosts
final_name = i3
apply_path = $outdir:/.config/
parse_files = true
recursive = true
extensions = i3config

[i3status-rust/config.toml]
hostnames = $hosts
final_name = config.toml
apply_path = $outdir:/.config/i3status-rust/

[nvim]
hostnames = $host
final_name = nvim
apply_path = $outdir:/.config/
parse_files = false
recursive = true
extensions = t

[redshift/redshift.conf]
hostnames = $hosts
final_name = redshift.conf
apply_path = $outdir:/.config/redshift/

[eww]
hostnames = $hosts
final_name = eww
apply_path = $outdir:/.config/
parse_files = true
recursive = true
extensions = t

[cwm/config]
hostnames = $hosts
final_name = config
apply_path = $outdir:/.config/crubwm/

[wezterm/wezterm.t.lua]
hostnames = $hosts
final_name = wezterm.lua
apply_path = $outdir:/.config/wezterm/

[kitty/kitty.t.conf]
hostnames = $hosts
final_name = kitty.conf
apply_path = $outdir:/.config/kitty/

[services/syncthing.service]
hostnames = $hosts
final_name = syncthing.service
apply_path = $outdir:/.config/systemd/user/

[services/ssh-agent.service]
hostnames = $hosts
final_name = ssh-agent.service
apply_path = $outdir:/.config/systemd/user/

[newsraft/feeds]
hostnames = $hosts
final_name = feeds
apply_path = $outdir:/.config/newsraft/

[hypr/hyprland.t.conf]
hostnames = $hosts
final_name = hyprland.conf
apply_path = $outdir:/.config/hypr/

[scripts/monsetup]
hostnames = $host
final_name = monsetup
apply_path = $outdir:/.local/bin/
parse_files = false
recursive = false

[scripts/pyb]
hostnames = $host
final_name = pyb
apply_path = $outdir:/.local/bin/
parse_files = false
recursive = false

[scripts/timer]
hostnames = $host
final_name = timer
apply_path = $outdir:/.local/bin/
parse_files = false
recursive = false

[vim/vimrc]
hostnames = $hosts
final_name = .vimrc
apply_path = $outdir

[rio/config.toml]
hostnames = $hosts
final_name = config.toml
apply_path = $outdir:/.config/rio/

[rio/gruvbox.toml]
hostnames = $hosts
final_name = gruvbox.toml
apply_path = $outdir:/.config/rio/themes/

[lazygit/config.yml]
hostnames = $hosts
final_name = config.yml
apply_path = $outdir:/.config/lazygit/

[ghostty/config]
hostnames = $hosts
final_name = config
apply_path = $outdir:/.config/ghostty/
